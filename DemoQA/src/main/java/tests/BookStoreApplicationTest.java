package tests;

public class BookStoreApplicationTest extends TestCase {
	/**
	 * TextBox - TC01
	 */
//	@Test
	public void doTC01Login() {
		// Click vào phần tử
//		HomePage homePage = new HomePage(testBasic.driver);
//		BookStoreApplicationPage bookStoreApplicationPage = homePage.clickBookStoreApplication();
//		testBasic.scrollToElement(bookStoreApplicationPage.menuLogin);
//		LoginPage loginPage = bookStoreApplicationPage.clickLoginMenu();
//
//		loginPage.testBasic.clickByLocator(loginPage.btnNewUser);
//
//		String inputFirstName = "pham";
//		String inputLastName = "cong";
//		String inputUserName = "cong";
//		String inputPassword = "1234";
//
//		loginPage.testBasic.inputText(loginPage.txtFirstName, inputFirstName);
//		loginPage.testBasic.inputText(loginPage.txtLastName, inputLastName);
//		loginPage.testBasic.inputText(loginPage.txtUserName, inputUserName);
//		loginPage.testBasic.inputText(loginPage.txtPassword, inputPassword);

//		testBasic.driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));

//		testBasic.clickByLocator(loginPage.iCaptcha);

//		WebDriverWait wait = new WebDriverWait(testBasic.driver, Duration.ofSeconds(15));
//		wait.until(ExpectedConditions.frameToBeAvailableAndSwitchToIt(loginPage.iCaptcha));
//		wait.until(ExpectedConditions.elementToBeClickable(loginPage.iCheckCaptcha)).click();

	}
	
	/**
	 * TextBox - TC02
	 */
//	@Test
	public void doTC02Login() {
		// Click vào phần tử
//		HomePage homePage = new HomePage(testBasic.driver);
//		BookStoreApplicationPage bookStoreApplicationPage = homePage.clickBookStoreApplication();
//		testBasic.scrollToElement(bookStoreApplicationPage.menuLogin);
//		LoginPage loginPage = bookStoreApplicationPage.clickLoginMenu();
//
//		loginPage.testBasic.clickByLocator(loginPage.btnNewUser);
//
//		String inputFirstName = "pham";
//		String inputLastName = "cong";
//		String inputUserName = "cong";
//		String inputPassword = "1234";
//
//		loginPage.testBasic.inputText(loginPage.txtFirstName, inputFirstName);
//		loginPage.testBasic.inputText(loginPage.txtLastName, inputLastName);
//		loginPage.testBasic.inputText(loginPage.txtUserName, inputUserName);
//		loginPage.testBasic.inputText(loginPage.txtPassword, inputPassword);

//		testBasic.driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));

//		testBasic.clickByLocator(loginPage.iCaptcha);

//		WebDriverWait wait = new WebDriverWait(testBasic.driver, Duration.ofSeconds(15));
//		wait.until(ExpectedConditions.frameToBeAvailableAndSwitchToIt(loginPage.iCaptcha));
//		wait.until(ExpectedConditions.elementToBeClickable(loginPage.iCheckCaptcha)).click();

	}

}
